<h1>Task Overview</h1>

<% @tasks.each do |task| %>
  <p><input type="checkbox" <%= task.completed == true ? "checked" : "" %>><%= link_to task.title, task_path(task) %></p>
<% end %>

<p><%= link_to "create new task", tasks_new_path %></p>
